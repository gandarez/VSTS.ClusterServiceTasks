{
	"id": "27eefdc3-c314-41af-8820-d6f58925963b",
	"name": "StartGenericServiceCluster",
	"friendlyName": "Windows Service Cluster - Start",
	"description": "Start a windows service in a cluster service",
	"author": "Carlos Henrique Gandarez",
	"helpMarkDown": "",
	"category": "Deploy",
	"visibility": [
		"Release"
	],	
	"version": {
		"Major": "1",
		"Minor": "0",
		"Patch": "0"
	},	
	"instanceNameFormat": "Start windows service $(resourceName) on $(clusterName)",
	"inputs": [
		{
			"name": "clusterName",
			"type": "string",
			"label": "Cluster Name",
			"defaultValue": "",
			"required": true,
			"helpMarkDown": "Name of the cluster."
		},
		{
			"name": "resourceName",
			"type": "string",
			"label": "Service Name",
			"defaultValue": "",
			"required": true,
			"helpMarkDown": "Name of the Windows Service (this is not always the display name)."
		},
		{
            "name": "WaitTimeoutInSeconds",
            "type": "string",
            "label": "Wait timeout",
            "defaultValue": "30",
            "required": true,
            "helpMarkDown": "The length of time, in seconds, that the task should wait for the resource to come online."
        }
	],
	"execution": {
		"PowerShell": {
			"target": "$(currentDirectory)\\StartGenericServiceCluster.ps1",
			"argumentFormat": "",
			"workingDirectory": "$(currentDirectory)"
		}
	}
}